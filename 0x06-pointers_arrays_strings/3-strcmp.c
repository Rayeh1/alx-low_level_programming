#include "main.h"
#include <string.h>

/**
 * _strcmp - function
 * @s1: string 1
 * @s2: string 2
 * Return: 0 (if same)
 */
int _strcmp(char *s1, char *s2)
{
	int ret;

	ret = strcmp(s1, s2);
	return (ret);
}
