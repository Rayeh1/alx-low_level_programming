#include "main.h"

/**
 * _strcmp - function
 * @s1: string 1
 * @s2: string 2
 * Return: 0 (if same)
 */
int _strcmp(char *s1, char *s2)
{
	int a;
	int b;

	s1[a];
	s2[b];
	if (a > b)
	{
		return (a - b);
	}
	else if (b > a)
	{
		return (b - a);
	}
	else
	{
		return('0');
	}
}
