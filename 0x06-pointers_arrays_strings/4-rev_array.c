#include "main.h"

/**
 * reverse_array - function
 * @n: elements
 * @a: string
 * Return:void
 */
void reverse_array(int *a, int n)
{
	int i;
	int *x;
	int z;
	int b;

	for (b = 0; b < n; b++)
		a[b];
		x[z] = a[b];

	i = n - 1;
	for (z = i; z >= 0; z--)
		x[z];
		a[b] = x[z];
}
