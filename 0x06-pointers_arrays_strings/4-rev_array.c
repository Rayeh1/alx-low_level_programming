#include "main.h"

/**
 * reverse_array - function
 * @n: elements
 * @a: string
 * Return:void
 */
void reverse_array(int *a, int n)
{
	int i;
	int *x;
	int y;

	x[y] = a[n];
	i = y - 1;

	for (y = i; y >= 0; y++)
		{
		a[n] = x[y];
		}
}
