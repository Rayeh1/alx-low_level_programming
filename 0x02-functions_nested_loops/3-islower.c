#include "main.h"

/**
 *islower - checks for lowercase characters
 *Return: Return 1 if c is lowercase, else return 0 if otherwise
 */
int _islower(int c)
{
	if (c = 'a'-'z')
		Return (1);
	
	else
	       	Return (0);
	
}
