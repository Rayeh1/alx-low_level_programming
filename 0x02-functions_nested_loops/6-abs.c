#include "main.h"

/**
 *int _abs -  computes the absolute value of an integer
 *Return: absolute number of integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
	_putchar('\n');
}	
